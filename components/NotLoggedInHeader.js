const NotLoggedInHeader = () => {
  return (
    <nav className="sticky top-0 w-full h-16 shrink-0 z-10 bg-gray-300 flex justify-center items-center animate-fade-in">
      Not logged in navbar
    </nav>
  );
};

export default NotLoggedInHeader;
